<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Event Management App</title>
</head>

<body>
    <%- include('./partials/navbar') %>
        <div class="container">
            <br>
            <div class="text-center">
                <h2>Update Event</h2>
            </div>
            <br>
            <div class="text-center">
                <% if (event) { %>
                    <form action="/editEvent/<%= event.Event_ID %>" method="POST" enctype="multipart/form-data">
                        <label for="Title">Event Title:</label><br>
                        <input type="text" id="Title" name="Title" value="<%= event.Title %>" required><br><br>

                        <label for="Description">Description:</label><br>
                        <input type="text" id="Description" name="Description" value="<%= event.Description %>"
                            required><br><br>

                        <label for="Start_Date">Start Date:</label><br>
                        <input type="datetime-local" id="Start_Date" name="Start_Date"
                            value="<%= new Date(event.Start_Date).toISOString().substring(0, 16) %>" required><br><br>

                        <label for="End_Date">End Date:</label><br>
                        <input type="datetime-local" id="End_Date" name="End_Date"
                            value="<%= new Date(event.End_Date).toISOString().substring(0, 16) %>" required><br><br>

                        <label for="Location">Location:</label><br>
                        <input type="text" id="Location" name="Location" value="<%= event.Location %>" required><br><br>

                        <label for="Image">Current Image:</label><br>
                        <input type="hidden" name="currentImage" value="<%= event.Image %>">
                        <img src="../images/<%= event.Image %>" width="20%"><br><br>

                        New Image:<br> <input type="file" id="Image" name="Image" accept="image/*"><br><br>
                        <button type="submit">Update Event</button>
                    </form>
                    <% } else { %>
                        <p>No event found.</p>
                        <% } %>
                            <a href="/">Back</a>
            </div>
        </div>
</body>

</html>