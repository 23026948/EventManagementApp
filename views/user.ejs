<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Attendee Details</title>
</head>

<body>
    <%- include('./partials/navbar') %>
        <div class="container">
            <br>
            <div class="text-center">
                <h2>Attendee Details</h2>
            </div>
            <br>
            <% if (user) { %> <!-- Check if data exists -->
                <div>
                    <p><b>Username:</b><br>
                        <%= user.Username %>
                    </p>
                    <p><b>Profile Picture:</b><br>
                        <img src="../images/<%= user.Profile_Picture %>" width="45%">
                    </p>
                    <p><b>Email:</b><br>
                        <%= user.Email %>
                    </p>
                    <p><b>Contact:</b><br>
                        <%= user.Contact %>
                    </p>
                    <p><b>Date of Birth:</b><br>
                        <%= user.DOB %>
                    </p>
                </div>
                <% } else { %>
                    <p>No user found.</p>
                    <% } %>
                        <a href="/deleteUser/<%= user.User_ID %>"
                            onclick="return confirm('Are you sure you want to delete attendee ' +  `<%= user.Username %>` + '?')">Delete attendee</a><br><br
                        <a href="/">Back</a>
        </div>

</body>

</html>